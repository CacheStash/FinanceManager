import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      manifest: {
        name: 'Finance Manager',
        short_name: 'FinManager',
        description: 'Aplikasi pengelola keuangan pribadi',
        theme_color: '#ffffff', // Sesuaikan dengan warna tema Anda
        background_color: '#ffffff',
        display: 'standalone', // INI KUNCINYA agar tidak ada bar browser
        scope: '/',
        start_url: '/',
        icons: [
          {
            src: 'pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          }
        ]
      }
    })
  ],
})